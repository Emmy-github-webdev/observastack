# Reference/validation fixture. Not part of the base Kustomization.
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: observastack-internal-tls
  namespace: observastack
spec:
  secretName: observastack-internal-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - api.internal.observastack
