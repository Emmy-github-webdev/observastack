apiVersion: v1
kind: ServiceAccount
metadata:
  name: external-secrets
  namespace: external-secrets
  labels:
    app.kubernetes.io/name: external-secrets
# AWS authentication is supplied by EKS Pod Identity.
# No static credentials and no IRSA annotation.
