# Reference fixture. Replace key with the Terraform-created secret name.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: observastack-rds
  namespace: observastack
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secretsmanager
    kind: SecretStore
  target:
    name: observastack-rds
    creationPolicy: Owner
    deletionPolicy: Retain
  dataFrom:
    - extract:
        key: REPLACE_WITH_STAGING_RDS_SECRET_NAME
