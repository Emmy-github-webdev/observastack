# Dev cert-manager overlay

Public API hostname: `api.dev.emmanuelogah.com`

ALB HTTPS uses the ACM certificate owned by Step 6. cert-manager is available for Kubernetes-native certificate lifecycle.
