apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: aws-secretsmanager
  namespace: observastack
spec:
  provider:
    aws:
      service: SecretsManager
      region: us-east-1
      # Pod Identity supplies controller credentials.
      # Do not add auth.secretRef or serviceAccountRef.
